using System.Runtime.Serialization;
using System.Xml;
using LearningPlatform.Models.UserModels;

namespace LearningPlatform
{
    public class TestSerialize
    {
        void test()
        {
            // var dcs = new DataContractSerializer(typeof(Student));
            // var xmlw = XmlWriter.Create("testfile.xml");
            // dcs.WriteObject(xmlw, obj);
            // xmlw.Close();
            // in classes
            // [DataContract]
            // [DataMember]
            
        }
    }
}